{
  "name": "ace-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter=* dev",
    "dev:api": "bun run --filter=@ace/api dev",
    "dev:cli": "bun run --filter=@ace/cli dev",
    "build": "bun run --filter=* build",
    "build:api": "bun run --filter=@ace/api build",
    "build:cli": "bun run --filter=@ace/cli build",
    "start:api": "bun run --filter=@ace/api start",
    "typecheck": "bun run --filter=* typecheck",
    "typecheck:api": "bun run --filter=@ace/api typecheck",
    "typecheck:cli": "bun run --filter=@ace/cli typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
